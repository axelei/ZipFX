#ifndef ZIPFX_PASSWORDMANAGERDIALOG_H
#define ZIPFX_PASSWORDMANAGERDIALOG_H

#include <QDialog>

class PasswordManagerDialog : public QDialog
{
    Q_OBJECT
public:
    explicit PasswordManagerDialog(QWidget* parent = nullptr);
};

#endif
