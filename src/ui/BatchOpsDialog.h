#ifndef ZIPFX_BATCHOPSDIALOG_H
#define ZIPFX_BATCHOPSDIALOG_H

#include <QDialog>

class BatchOpsDialog : public QDialog
{
    Q_OBJECT
public:
    explicit BatchOpsDialog(QWidget* parent = nullptr);
};

#endif
